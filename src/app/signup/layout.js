export const metadata = {
    title: "Sign up",
    description:
        "Create a free ShrinkVerse account for permanent short links, custom aliases, and a searchable link history.",
    alternates: { canonical: "/signup" },
    robots: { index: true, follow: true },
};

export default function SignupLayout({ children }) {
    return children;
}
