export const metadata = {
    title: "Shorten a link",
    description:
        "Paste a long URL, pick a custom alias, and get a short ShrinkVerse link in seconds. Free, no sign-up required for 15-day links.",
    alternates: { canonical: "/shorten" },
};

export default function ShortenLayout({ children }) {
    return children;
}
