export const metadata = {
    title: "Login",
    description:
        "Sign in to ShrinkVerse to manage your links, edit destinations, and view your link history.",
    alternates: { canonical: "/login" },
    robots: { index: true, follow: true },
};

export default function LoginLayout({ children }) {
    return children;
}
