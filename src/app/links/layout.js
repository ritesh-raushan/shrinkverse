export const metadata = {
    title: "My links",
    description: "View, copy, and manage the links you've created with ShrinkVerse.",
    robots: { index: false, follow: false },
};

export default function LinksLayout({ children }) {
    return children;
}
